// Levain — client config. Filled in once the Worker is deployed; the app
// runs fine local-only with these left blank (no cross-device sync).
window.WORKER_URL = ""; // e.g. "https://levain-worker.<you>.workers.dev"
window.APP_KEY = ""; // must match the Worker's APP_KEY secret
