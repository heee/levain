// Levain — client config. Filled in once the Worker is deployed; the app
// runs fine local-only with these left blank (no cross-device sync).
window.WORKER_URL = "https://levain-worker.jhenningbuchholz.workers.dev";
window.APP_KEY = "0J_VX_gB9qprhmgCLMVrkfBW_b4tSElo"; // matches the Worker's APP_KEY secret — casual deterrent only, not real auth
